# Dependencies
node_modules

# Build output — compiled bundles are release artifacts, not source.
main.js
packages/*/dist
*.map

# The sync server's secrets never belong in the repository.
packages/server/.env

# Per-machine deployment choices, such as opening the port to a network.
packages/server/docker-compose.override.yml

# Obsidian writes plugin settings here at runtime.
data.json

# Test output
coverage

# Editors
.vscode
.idea
*.iml

# OS
.DS_Store
Thumbs.db
